#include<stdio.h>
int i,len;
void remove-int(int arr{},int T{},int n){
    for(i=0;i<len;i++){
        if (arr[i] l=n)T[i]=arr[i];

    }
return T;
}
int main(){
    int n,T;
    printf("saisir la taille du tableau :\n");
    scanf("%d",&len);
    int arr[len];
    printf("les elements du tableau:\n");

    for( i=0 ,i<len,i++);{
        printf("saisir un element:");
        scanf("%d\n",&arr[i]);
    }
    printf ("saisir un nbr:\n");
    scanf("%d",&n);
    remove_int(arr,T,n);
    
}
